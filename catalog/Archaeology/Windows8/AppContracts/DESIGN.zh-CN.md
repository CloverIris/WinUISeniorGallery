# App Contracts 设计考古与现代化

保留 DNA：明确用户意图、调用前说明将共享什么、回到原任务后保留上下文。拒绝隐式后台分享、跨应用数据探测和系统栏替代应用内确认。窄窗使用顺序图，宽窗显示消息流；主题/高对比度确保状态与错误不只靠颜色，RTL 按逻辑方向排列。键盘按意图、上下文、结果顺序；Reduced Motion 省略连线动画；Narrator 宣告数据类别与“未实际发送”。
