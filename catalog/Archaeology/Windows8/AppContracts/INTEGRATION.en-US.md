# App Contracts Research Exhibit Integration

Demo data is hard-coded, non-personal, read-only labels; it calls no DataTransferManager, picker, account, network, or capability. Unload cancels local state and UI updates occur on the UI thread; absent platform/modern owner only shows unavailable. Use Gallery theme resources and self-made flow diagrams, not system screenshots/icons; no telemetry, user data, or external provider.
