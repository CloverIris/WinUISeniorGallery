# App Contracts 研究展示验收

- Given 展项打开，When 选择 Share 或 Picker，Then 显示完整手交流程和“未执行平台调用”。
- Given `Unavailable` 或 `Cancelled`，When 返回，Then 焦点回到意图触发器且无数据持久化。
- Given 中文/英文/RTL、200% DPI、High Contrast、键盘或 Reduced Motion，When 浏览状态，Then 文本、顺序、错误和焦点均可感知。

自动化验证路由、状态、无 capability/网络/账户调用与本地首帧；只允许经审查自制资产。
