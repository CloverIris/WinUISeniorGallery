# Semantic Zoom 设计考古与现代化

保留 DNA：概览是导航层级，用户在返回时保有位置上下文。拒绝把动画缩放误作语义导航、只依赖双指手势、无标签的缩略图。现代 owner 应有明确“概览/详情”可达入口和可读分组标签。

窄窗用切换按钮而非捏合；常规/宽窗可并列解释。Light/Dark/High Contrast 让选中组、焦点和文本独立可辨；RTL 逻辑排序由语言/数据决定；100/150/200% DPI 不截断索引。Reduced Motion 使用即时层级切换；Tab、方向键与 Narrator 必须报告当前层级、组名和返回目标。
