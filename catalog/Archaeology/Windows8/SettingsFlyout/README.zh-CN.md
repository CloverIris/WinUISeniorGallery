# Settings Flyout（Windows 8 设计考古）

研究 Windows 8 从 Charms Settings 打开的应用内设置 flyout：保持当前内容可见，同时在边缘进入设置层级。现代 owner 为 `windowing.settings-panel`；本目录只拥有研究，状态 `proposed` / `lab` / `P2`、许可 `pending`、路由 `/archaeology/windows-8/settings-flyout`。其余文档覆盖状态、现代取舍、宿主边界、验收与来源；不允许直接实现。
