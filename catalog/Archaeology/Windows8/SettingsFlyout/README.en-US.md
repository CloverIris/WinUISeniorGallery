# Settings Flyout (Windows 8 Design Archaeology)

This item studies the Windows 8 app settings flyout opened from Charms Settings: current content remains visible while settings enter at the edge. Modern owner is `windowing.settings-panel`; this folder owns research only, status `proposed` / `lab` / `P2`, license `pending`, route `/archaeology/windows-8/settings-flyout`. The remaining documents cover state, modernization, host boundary, acceptance, and sources; direct implementation is not allowed.
