# Settings Flyout Research Exhibit Integration

Only link `windowing.settings-panel` documentation and use local read-only demo items; do not write user settings, create windows, request capabilities, call system Settings, or network. All demo state is on the UI thread; unload cancels motion and discards dirty model; host rejection, missing backdrop, or narrow window falls back visibly. Resources are theme keys and self-made geometry only; no Microsoft screenshots/icons or telemetry.
