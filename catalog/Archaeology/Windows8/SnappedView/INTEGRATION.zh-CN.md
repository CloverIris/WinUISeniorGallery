# Snapped View 研究展示集成

本页仅读取 `windowing.dock-layout-preview` 的公开规格并链接到 owner；不调用 AppWindow、窗口迁移、Shell 或平台权限。演示模型为页面本地只读数据，UI 线程更新；卸载取消模拟拖拽/动画，宿主窗口关闭时不保留状态。无能力、拒绝、异常或 backdrop 不可用均呈现本地说明和诊断，不联网、不遥测、不处理用户文件。

使用 Gallery 主题资源与自制几何图，禁止 Windows 截图/图标/壁纸/提取资产；外部证据链接须标明离开应用。
