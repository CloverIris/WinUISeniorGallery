# Zune Big Typography Acceptance

## Research and Boundary

Given page load, when Owner/Sources is inspected, then show controls.big-title, BigTitle, proposed/pending, and Modern Reconstruction/not Microsoft. The exhibit has no public API or stable resource key.

## Prototype and Modern Difference

Given the prototype diagram, when switching to Modern Demo, then preserve Design DNA while explicitly listing discarded behavior; never present modern behavior as historical fact.

## Given / When / Then

Given long Chinese title at 200% and 320 epx, then no clipping/horizontal scroll and body remains reachable. Given Reduced Motion, Expanded/Compact switches instantly.

## Input, Accessibility, and Responsive Behavior

Title is noninteractive and out of Tab by default; if bound to navigation it gets button semantics/focus and no invisible hit area. CJK uses suitable weight/scale, German wraps, RTL mirrors alignment; 200% text allows multiline/compact instead of clipping. Verify Light/Dark/High Contrast, Reduced Motion, Narrator, mouse/keyboard/touch/pen/gamepad, RTL, 100%–300% DPI, and 200% text.

## Lifecycle and Performance

Static explanation first frame targets under 100 ms; Demo targets 60 Hz and under 4 ms UI/frame, with no animation/Provider/event after unload. If budget fails, retain static demo.

## Copyright Gate

Automated checks find no historical screenshot/sound/font/extracted resource; SOURCES are reachable and include 2026-07-16. Do not advance to preview/stable before license_review passes.

