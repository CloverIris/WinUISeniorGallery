# Zune Mixview Acceptance

## Research and Boundary

Given page load, when Owner/Sources is inspected, then show experiences.mixview, MixviewExperience, proposed/pending, and Modern Reconstruction/not Microsoft. The exhibit has no public API or stable resource key.

## Prototype and Modern Difference

Given the prototype diagram, when switching to Modern Demo, then preserve Design DNA while explicitly listing discarded behavior; never present modern behavior as historical fact.

## Given / When / Then

Given 40 relations, when loaded, then rules select at most 12 first-view nodes with paging. Given Narrator, list order matches visual relation categories.

## Input, Accessibility, and Responsive Behavior

Arrows/gamepad move spatially, Enter/A recenters, Escape/B returns prior center; provide linear relation list for Narrator/2D-inaccessible users. Narrow reduces rings and uses bottom details; wide expands radius with node cap; text scaling grows nodes rather than compressing labels. Verify Light/Dark/High Contrast, Reduced Motion, Narrator, mouse/keyboard/touch/pen/gamepad, RTL, 100%–300% DPI, and 200% text.

## Lifecycle and Performance

Static explanation first frame targets under 100 ms; Demo targets 60 Hz and under 4 ms UI/frame, with no animation/Provider/event after unload. If budget fails, retain static demo.

## Copyright Gate

Automated checks find no historical screenshot/sound/font/extracted resource; SOURCES are reachable and include 2026-07-16. Do not advance to preview/stable before license_review passes.

