# Voice Access Overlay Acceptance

## Research and Ownership

Given page load, when metadata is inspected, then show proposed/pending, owner unassigned (no stable API), Modern Reconstruction/not Microsoft, and no stable exhibit API.

## Historical and Modern Difference

Given historical structure, when switching Demo, then preserve Design DNA and state discarded boundary without imitating Windows Shell/system capability.

## Given / When / Then

Given research Demo, when Show numbers is activated, then label invokable demo elements only, capture no audio, and change no system focus. Given Escape, labels disappear and trigger focus restores.

## Input, High Contrast, RTL, and Automation

Buttons/keyboard simulate Listening, Numbers, and Grid without global hotkeys. Focus never moves to number labels; Escape closes innermost overlay and restores trigger. Number labels avoid targets/edges and grid recomputes for local demo bounds; RTL affects text, not spoken number order. High Contrast uses solid borders and labels do not obscure focus. Verify Light/Dark/High Contrast, Reduced Motion, Narrator, mouse/keyboard/touch/pen/gamepad, RTL, 100%–300% DPI, and 200% text.

## Lifecycle and Performance

Static explanation first frame under 100 ms; Demo targets 60 Hz/under 4 ms UI frame with no Provider/timer/event after unload; otherwise retain static research.

## Copyright Gate

Automated checks find no Windows 11 screenshot/system icon/sound/font/news/extracted asset; SOURCES preserve 13-file shape. Do not advance preview/stable before license_review passes.

