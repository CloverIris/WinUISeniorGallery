# Widgets Board Acceptance

## Research and Ownership

Given page load, when metadata is inspected, then show proposed/pending, owner experiences.widgets-board (WidgetsBoard), Modern Reconstruction/not Microsoft, and no stable exhibit API.

## Historical and Modern Difference

Given historical structure, when switching Demo, then preserve Design DNA and state discarded boundary without imitating Windows Shell/system capability.

## Given / When / Then

Given three widgets with one refresh failure, when Board opens, then others remain usable and failed card retries locally. Given keyboard reorder, persist once and focus follows ID.

## Input, High Contrast, RTL, and Automation

Keyboard/gamepad uses 2D card navigation, menu opens actions, and drag has keyboard alternative; close restores entry focus and refresh never steals focus. Narrow one column, regular two, wide three/four; 200% text grows card height. RTL mirrors grid while time/chart semantics follow locale. Verify Light/Dark/High Contrast, Reduced Motion, Narrator, mouse/keyboard/touch/pen/gamepad, RTL, 100%–300% DPI, and 200% text.

## Lifecycle and Performance

Static explanation first frame under 100 ms; Demo targets 60 Hz/under 4 ms UI frame with no Provider/timer/event after unload; otherwise retain static research.

## Copyright Gate

Automated checks find no Windows 11 screenshot/system icon/sound/font/news/extracted asset; SOURCES preserve 13-file shape. Do not advance preview/stable before license_review passes.

