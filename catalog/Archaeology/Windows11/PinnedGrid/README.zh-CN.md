# Start Menu Pinned Grid

Windows 11 Start 的 Pinned 区使用居中、自适应应用图标网格，支持分页、拖拽重排，并在后续版本加入文件夹组合；Recommended 与 Pinned 分区。

## 状态与所有权

- 状态：proposed / lab
- 现代 owner：controls.adaptive-grid（AdaptiveGrid）
- license_review：pending

考古项只拥有研究、现代化说明和 Gallery 配置，不新增稳定 API、不模拟 Windows Shell。

## Gallery 展示结构

历史结构图 → 状态/输入 → Design DNA → 舍弃边界 → AdaptiveGrid Demo → 无障碍/响应式矩阵 → Sources/Copyright。

