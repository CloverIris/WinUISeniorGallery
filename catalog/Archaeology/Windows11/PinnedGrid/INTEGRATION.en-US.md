# Start Menu Pinned Grid Integration

## Owner and Dependency Boundary

Modern owner: controls.adaptive-grid (AdaptiveGrid). AdaptiveGrid owns layout/virtualization; host experience owns folders, reorder transaction, and launch commands, and the exhibit owns no such API. The exhibit duplicates no owner API/resource and stable modules do not depend on Archaeology.

## Gallery Data and Platform Exclusion

Structure/DNA/differences come from local Markdown and demo uses anonymous deterministic fake data. Access no Shell, taskbar, Start, real pins, account, microphone, or global input.

## Window, Input, and Lifecycle

Demo stays in current Gallery XamlRoot. Arrows/gamepad navigate grid, Enter/A invokes, menu manages, and keyboard alternatives move/create folder; icons expose text names rather than graphics only. Unload cancels animation/Provider/timer, ignores late results, and restores navigation focus.

## Degradation and Automation

Missing owner/effect retains structure diagram, state diagram, and Sources; High Contrast/Reduced Motion/screen reader have static equivalent paths.

## Copyright and Privacy

license_review remains pending; assets record author/license/provenance. Collect no query, pin, task, voice, or interaction telemetry.

