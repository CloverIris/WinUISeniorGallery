# Media Center Grid Acceptance

## Research and Boundary

Given page load, when Owner/Sources is inspected, then show media.media-center-grid, MediaCenterGrid, proposed/pending, and Modern Reconstruction/not Microsoft; no public API/stable resource key.

## Prototype and Modern Difference

Given prototype structure, when switching Modern Demo, then preserve DNA and list discarded behavior without presenting modern behavior as history.

## Given / When / Then

Given 10,000 items focused on ID 42, when Window reflows 6 to 3 columns, then focus remains ID 42, only buffers realize, and details never flash wrong item.

## Input, Accessibility, and Responsive Behavior

D-pad/stick follows geometric neighbors, A/Enter invokes, B/Escape returns; mouse/touch/keyboard equal. Automation exposes row/column, title, selection, and details. Reflow 1–8 columns by minimum card width and retain stable-ID focus; text scaling reduces columns, not font; High Contrast uses system focus ring. Verify themes, Reduced Motion, Narrator, all input, RTL, 100%–300% DPI, and 200% text.

## Lifecycle and Performance

Static explanation first frame targets under 100 ms; Demo 60 Hz/under 4 ms UI frame with no animation/Provider/event after unload; otherwise retain static Demo.

## Copyright Gate

Automated check finds no historical screenshot/sound/font/extracted resource; SOURCES reachable with 2026-07-16. Do not advance preview/stable before license_review passes.

