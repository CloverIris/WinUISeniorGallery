# Media Center Overlay Menu Acceptance

## Research and Boundary

Given page load, when Owner/Sources is inspected, then show controls.overlay-menu, OverlayMenu, proposed/pending, and Modern Reconstruction/not Microsoft; no public API/stable resource key.

## Prototype and Modern Difference

Given prototype structure, when switching Modern Demo, then preserve DNA and list discarded behavior without presenting modern behavior as history.

## Given / When / Then

Given captions submenu focused on a language, when auto-hide expires, menu stays. Given B twice, return Root then close and restore video focus.

## Input, Accessibility, and Responsive Behavior

Menu/Enter/A opens, D-pad navigates, B/Escape backs by layer; mouse/touch/keyboard equal. Do not auto-hide with menu, focus, or child popup. Narrow uses bottom Sheet, wide center/side Overlay, 10-foot enlarges targets; CompactOverlay retains core commands only. Verify themes, Reduced Motion, Narrator, all input, RTL, 100%–300% DPI, and 200% text.

## Lifecycle and Performance

Static explanation first frame targets under 100 ms; Demo 60 Hz/under 4 ms UI frame with no animation/Provider/event after unload; otherwise retain static Demo.

## Copyright Gate

Automated check finds no historical screenshot/sound/font/extracted resource; SOURCES reachable with 2026-07-16. Do not advance preview/stable before license_review passes.

