# Pivot Integration

## Modern Owner and Dependency

The sole modern owner is controls.pivot-view (PivotView). The exhibit configures its demo only through registered public owner surfaces; stable modules never reference Archaeology.

## Gallery Data and Platform

Prototype, DNA, and differences come from local Markdown; demo uses anonymous deterministic fake data. It accesses no historical service, account, media library, or system Shell. Async assets are cancellable and carry license metadata.

## Input, Window, and Lifecycle

Demo belongs to current Gallery XamlRoot and creates/migrates no Window. Keep horizontal swipe; arrows/gamepad bumpers switch, Tab enters page content, screen reader announces item n of m. RTL mirrors visual order/direction. Unload cancels animation, delay, and Provider, ignores late results, and restores navigation focus.

## Degradation and Resources

When owner/effect is unavailable, retain text diagram, input matrix, and Sources. Archaeology never duplicates PivotView style keys/API; historical preset exists only in Gallery configuration.

## Copyright and Privacy

license_review remains pending; every demo asset records author, license, and provenance. Upload no user media/history and collect no exhibit interaction telemetry.

