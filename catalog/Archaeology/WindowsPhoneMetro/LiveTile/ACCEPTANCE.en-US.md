# Live Tile Acceptance

## Research and Boundary

Given page load, when Owner/Sources is inspected, then show controls.dynamic-tile, DynamicTile, proposed/pending, and Modern Reconstruction/not Microsoft. The exhibit has no public API or stable resource key.

## Prototype and Modern Difference

Given the prototype diagram, when switching to Modern Demo, then preserve Design DNA while explicitly listing discarded behavior; never present modern behavior as historical fact.

## Given / When / Then

Given a wide Tile with two pages, when Reduced Motion, then content swaps instantly with one announcement. Given stale data, show static fallback and keep invoke.

## Input, Accessibility, and Responsive Behavior

Click/Enter/A invokes and outer Board owns keyboard/touch reorder; dynamic content does not rename focused tile and screen reader announces aggregate state, not every flip. Sizes use modern grid breakpoints, not WP pixels; narrow has at least two columns and text scaling grows height or hides secondary copy. Verify Light/Dark/High Contrast, Reduced Motion, Narrator, mouse/keyboard/touch/pen/gamepad, RTL, 100%–300% DPI, and 200% text.

## Lifecycle and Performance

Static explanation first frame targets under 100 ms; Demo targets 60 Hz and under 4 ms UI/frame, with no animation/Provider/event after unload. If budget fails, retain static demo.

## Copyright Gate

Automated checks find no historical screenshot/sound/font/extracted resource; SOURCES are reachable and include 2026-07-16. Do not advance to preview/stable before license_review passes.

