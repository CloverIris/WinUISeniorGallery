# MediaPlayback 设计

## 信息层级

该工作单元没有视觉树。快照从会话身份、状态和能力开始，再表达时间、音频参数与错误；控件依据能力决定可见或禁用的操作。

## 交互与可访问性

契约不处理输入或焦点。所有 Consumer 必须根据快照提供键盘、屏幕阅读器、主题、RTL 和 Reduced Motion 行为；错误代码不能直接作为面向用户文本。

## 现代化取舍

不以静态全局播放器或可变单例表示状态。按窗口拥有会话与 Dispatcher，既允许 `MediaPlayerElement`、C++ 或测试 Fake 适配，也避免跨窗口 XAML 泄漏。
