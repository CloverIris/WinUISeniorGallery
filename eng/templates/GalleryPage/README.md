# Gallery page work-item template

Use `catalog/GalleryPages/HomePage` as the Gallery-page work-item template. Gallery pages own navigation, examples, and teaching content, never reusable public APIs.
