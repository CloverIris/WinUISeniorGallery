<Page
    x:Class="WinUI3_Senior_Gallery.Pages.PeopleCardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WinUI3.Senior.Controls">
    <Grid Margin="28" RowSpacing="16">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <StackPanel Spacing="4">
            <TextBlock Text="PeopleCard" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Host-owned contact summary and action surface" Style="{StaticResource SubtitleTextBlockStyle}" />
            <TextBlock Text="合成联系人；动作只发事件，不连接通讯录、账号或网络服务。" Opacity="0.7" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <Button Content="Toggle details" Click="OnToggleClick" />
            <Button Content="Message" Click="OnMessageClick" />
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="8,0,0,0" Opacity="0.7" />
        </StackPanel>
        <Border Grid.Row="2" MaxWidth="520" HorizontalAlignment="Left" Padding="1" CornerRadius="12">
            <controls:PeopleCard x:Name="Card" />
        </Border>
    </Grid>
</Page>
